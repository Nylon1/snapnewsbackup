<!DOCTYPE html>
<html lang="en">
<head>
  <title>Upload a Snap · SnapNews</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Segoe UI', sans-serif; }
    .input-file::-webkit-file-upload-button {
      visibility: hidden;
    }
    .input-file::before {
      content: 'Choose Video';
      display: inline-block;
      background: #ffd500;
      color: #181e29;
      border-radius: 4px;
      padding: 0.5rem 1rem;
      margin-right: 1rem;
      cursor: pointer;
      font-weight: 600;
    }
    .input-file:hover::before {
      background: #fff3b0;
    }
  </style>
</head>
<body class="bg-black text-white min-h-screen flex flex-col items-center justify-center">

  <div class="w-full max-w-md mt-10 bg-gray-900 rounded-lg shadow-lg p-8">
    <h2 class="text-2xl font-bold mb-2 text-yellow-400 text-center">Upload a Snap</h2>
    <p class="text-center text-gray-400 mb-6">Share your video with SnapNews!</p>
    
    <form action="/upload" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
      <input type="file" name="video" accept="video/mp4" required class="input-file w-full py-2 px-3 bg-gray-800 rounded mb-3">
      <input type="text" name="title" placeholder="Title (optional)" maxlength="90"
        class="py-2 px-3 rounded bg-gray-800 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-400">
      <button type="submit" class="bg-yellow-400 text-black font-semibold py-2 rounded hover:bg-yellow-300 transition">Upload Snap</button>
    </form>
    <div class="mt-6 text-center">
      <a href="/feed" class="text-yellow-400 hover:underline">Back to Feed</a>
    </div>
  </div>
</body>
</html>
