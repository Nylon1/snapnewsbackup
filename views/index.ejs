<!DOCTYPE html>
<html lang="en">
<head>
  <title>SnapNews – Home</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Segoe UI', sans-serif; }
  </style>
</head>
<body class="bg-black text-white min-h-screen flex flex-col">
  <!-- HEADER -->
  <header class="w-full py-8 flex flex-col items-center">
    <span class="text-6xl mb-2" aria-label="Fire Logo" title="SnapNews">🔥</span>
    <h1 class="text-4xl sm:text-5xl font-extrabold text-yellow-400 mb-2">SnapNews</h1>
    <p class="text-gray-300 text-base sm:text-lg max-w-md text-center mb-4">
      Share your story in 60 seconds. Watch it vanish in 24 hours.
    </p>
    <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
      <a href="/upload"
         class="bg-yellow-400 hover:bg-yellow-300 text-black font-bold px-6 py-3 rounded-full text-sm sm:text-base">
         Upload a Video
      </a>
      <a href="/feed"
         class="bg-white/10 hover:bg-white/20 text-white border border-white px-6 py-3 rounded-full text-sm sm:text-base">
         Watch Snaps
      </a>
    </div>
  </header>

  <!-- MAIN GRID -->
  <main class="flex-1 w-full flex justify-center">
    <div class="w-full max-w-5xl px-3 flex flex-col md:flex-row gap-8 items-start mx-auto">
      <!-- LEFT: Trending -->
      <section class="flex-1 w-full flex flex-col items-center md:items-start">
        <div class="bg-[#232b3c] rounded-2xl p-6 shadow-lg w-full max-w-xs mx-auto">
          <div class="text-yellow-400 text-xl font-semibold flex items-center gap-2 mb-4">
            <span>🔥</span> Trending Now
          </div>
          <!-- Dynamic Trending Snaps Loop (EJS example) -->
          <% if (trendingSnaps && trendingSnaps.length) { %>
            <% trendingSnaps.forEach(function(snap) { %>
              <div class="bg-[#343c4d] rounded-lg p-3 mb-3 flex flex-col items-start">
                <% if (snap.thumbnail) { %>
                  <img src="<%= snap.thumbnail %>" alt="Snap Thumbnail" class="w-full h-28 object-cover rounded mb-2" />
                <% } else { %>
                  <div class="w-full h-28 bg-[#232a3c] rounded mb-2"></div>
                <% } %>
                <div class="text-white text-base font-medium"><%= snap.title %> <span class="text-yellow-400"><%= snap.tags %></span></div>
              </div>
            <% }); %>
          <% } else { %>
            <div class="text-gray-400">No trending snaps yet.</div>
          <% } %>
        </div>
      </section>

      <!-- RIGHT: Snapper of the Week -->
      <section class="flex-1 w-full flex flex-col items-center md:items-end">
        <div class="bg-[#181e29] rounded-2xl p-8 shadow-lg w-full max-w-xs mx-auto">
          <div class="text-pink-400 text-xl font-semibold mb-4 flex items-center gap-2 justify-center">
            <span>🌟</span> Snapper of the Week
          </div>
          <% if (snapperOfTheWeek) { %>
            <div class="flex flex-col items-center">
              <img src="<%= snapperOfTheWeek.avatar %>" alt="Snapper Avatar" class="w-20 h-20 rounded-full border-4 border-pink-400 mb-3 bg-[#232b3c] object-cover" />
              <div class="font-bold text-lg mb-1 text-white"><%= snapperOfTheWeek.username %></div>
              <div class="text-gray-300 text-center text-base mb-3"><%= snapperOfTheWeek.bio %></div>
              <% if (snapperOfTheWeek.highlightVideoUrl) { %>
                <video controls class="rounded-md w-full mt-2" style="max-width:210px;">
                  <source src="<%= snapperOfTheWeek.highlightVideoUrl %>" type="video/mp4">
                </video>
              <% } %>
            </div>
          <% } else { %>
            <div class="flex flex-col items-center">
              <div class="w-20 h-20 rounded-full border-4 border-pink-400 flex items-center justify-center text-2xl text-yellow-400 mb-3 bg-[#232b3c]">?</div>
              <div class="font-bold text-lg mb-1 text-white">@SnapQueen</div>
              <div class="text-gray-300 text-center text-base">Uploaded the most loved Snap of the week 🎉</div>
            </div>
          <% } %>
        </div>
      </section>
    </div>
  </main>

  <!-- FOOTER / PROMPT -->
  <footer class="text-center px-4 py-10">
    <h2 class="text-lg sm:text-xl font-semibold text-yellow-400 mb-2">Add SnapNews to your Home Screen</h2>
    <p class="text-gray-400 max-w-md mx-auto text-sm">
      Tap your browser menu and choose "Add to Home Screen" for quick access.
    </p>
    <span class="text-4xl my-4 block">🔥</span>
    <p class="text-xs text-gray-600">SnapNews © 2025</p>
  </footer>
</body>
</html>
