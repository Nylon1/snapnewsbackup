<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>SnapNews · Home</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background: #07090f;
      color: #fff;
      font-family: 'Inter', Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    .main-content {
      display: flex;
      flex-direction: row;
      justify-content: flex-start;
      gap: 2rem;
      padding: 2.5rem 2rem 1.5rem 2rem;
      min-height: 100vh;
    }
    .left-column {
      flex: 2 1 0;
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }
    .right-column {
      flex: 1 1 320px;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
    }
    /* Snapper of the Week, Trending styles */
    .snapper-week, .trending {
      background: #181e29;
      border-radius: 20px;
      padding: 1.4rem 2rem;
      color: #fff;
      min-width: 280px;
      max-width: 340px;
      box-shadow: 0 2px 12px 0 #10131c2b;
    }
    .snapper-week {
      margin-left: 1rem;
    }
    .trending h2 {
      color: #ffd500;
      font-size: 1.3rem;
      margin: 0 0 0.7rem 0;
      display: flex;
      align-items: center;
      gap: 0.5em;
    }
    /* Trending Snap card */
    .trending-card {
      background: #242a37;
      border-radius: 13px;
      padding: 1rem 1rem 0.8rem 1rem;
      margin-bottom: 0.7rem;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      min-height: 120px;
    }
    .trending-thumb {
      width: 85px;
      height: 60px;
      border-radius: 8px;
      background: #232a3c;
      margin-bottom: 0.6rem;
      object-fit: cover;
    }
    /* Buttons */
    .upload-btn, .watch-btn {
      font-size: 1.1rem;
      font-weight: bold;
      padding: 0.95rem 2.2rem;
      border-radius: 28px;
      border: none;
      margin-right: 0.8rem;
      margin-bottom: 1rem;
      cursor: pointer;
      box-shadow: 0 2px 12px #181e291a;
      transition: background 0.14s;
    }
    .upload-btn { background: #ffd500; color: #181e29; }
    .upload-btn:hover { background: #fff055; }
    .watch-btn { background: #181e29; color: #fff; border: 2px solid #fff; }
    .watch-btn:hover { background: #232b3c; }
    .snapper-header {
      color: #ff7373;
      font-size: 1.25rem;
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 0.5em;
      margin-bottom: 0.7em;
    }
    .snapper-avatar {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background: #232b3c;
      margin: 0 auto 0.7rem auto;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.1rem;
      color: #ffd500;
      border: 3px solid #ff7373;
    }
    .snapper-name {
      text-align: center;
      font-weight: bold;
      font-size: 1.1rem;
      margin-bottom: 0.4rem;
    }
    .snapper-desc {
      text-align: center;
      font-size: 0.98rem;
      color: #cfd5e6;
    }
    @media (max-width: 900px) {
      .main-content {
        flex-direction: column;
        gap: 1.2rem;
        padding: 1.2rem;
        min-height: unset;
      }
      .right-column {
        align-items: stretch;
        margin-left: 0;
      }
      .snapper-week {
        margin-left: 0;
        margin-top: 1.2rem;
      }
      .left-column {
        gap: 1.3rem;
      }
    }
  </style>
</head>
<body>
  <div class="main-content">
    <!-- LEFT COLUMN -->
    <div class="left-column">
      <div>
        <a href="/upload"><button class="upload-btn">Upload a Video</button></a>
        <a href="/feed"><button class="watch-btn">Watch Snaps</button></a>
      </div>
      <div class="trending">
        <h2>🔥 Trending Now</h2>
        <div class="trending-card">
          <!-- You can render trending video info dynamically here -->
          <div class="trending-thumb"></div>
          <div style="color:#fff;font-size:1.07rem;">Funny Dog Jump <span style="color:#ffd500;">#pets</span></div>
        </div>
        <!-- Add more trending cards if desired -->
      </div>
    </div>
    <!-- RIGHT COLUMN -->
    <div class="right-column">
      <div class="snapper-week">
        <div class="snapper-header">🌟 Snapper of the Week</div>
        <div class="snapper-avatar">S</div>
        <div class="snapper-name">@SnapQueen</div>
        <div class="snapper-desc">Uploaded the most loved Snap of the week 🎉</div>
      </div>
    </div>
  </div>
</body>
</html>
