<h1 style="color:lime;text-align:center;">INDEX.EJS TEST — IT WORKS?</h1>


<!DOCTYPE html>
<html lang="en">
<head>
  <title>Upload a Snap · SnapNews</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Segoe UI', sans-serif; }
    .custom-file {
      display: flex;
      align-items: center;
      background: #23293a;
      border-radius: 8px;
      padding: 0.5rem 1rem;
      margin-bottom: 1.2rem;
      justify-content: space-between;
    }
    .custom-file label {
      background: #ffd500;
      color: #181e29;
      font-weight: 600;
      border-radius: 5px;
      padding: 0.5rem 1.2rem;
      cursor: pointer;
      transition: background 0.18s;
      margin-right: 1.2rem;
      border: none;
      outline: none;
    }
    .custom-file label:hover {
      background: #ffe150;
    }
    .custom-file input[type="file"] {
      display: none;
    }
    .file-name {
      color: #fff;
      font-size: 0.98rem;
      opacity: 0.8;
      word-break: break-all;
    }
  </style>
</head>
<body class="bg-black text-white min-h-screen flex flex-col items-center justify-center">

  <div class="w-full max-w-md mt-12 bg-[#161b24] rounded-2xl shadow-lg p-8">
    <h2 class="text-2xl font-extrabold mb-2 text-yellow-400 text-center">Upload a Snap</h2>
    <p class="text-center text-gray-400 mb-6">Share your video with SnapNews!</p>
    
    <form action="/upload" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
      <div class="custom-file">
        <label for="fileInput">Choose Video</label>
        <input id="fileInput" type="file" name="video" accept="video/mp4" required onchange="updateFileName()">
        <span class="file-name" id="fileName">No file chosen</span>
      </div>
      <input type="text" name="title" placeholder="Title (optional)" maxlength="90"
        class="py-2 px-3 rounded bg-gray-800 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-400 transition-all">
      <button type="submit" class="bg-yellow-400 text-black font-semibold py-2 rounded-lg hover:bg-yellow-300 transition text-lg">Upload Snap</button>
    </form>
    <div class="mt-6 text-center">
      <a href="/feed" class="text-yellow-400 hover:underline">← Back to Feed</a>
    </div>
  </div>
  <script>
    function updateFileName() {
      const input = document.getElementById('fileInput');
      const fileNameSpan = document.getElementById('fileName');
      fileNameSpan.textContent = input.files.length ? input.files[0].name : 'No file chosen';
    }
  </script>
</body>
</html>
